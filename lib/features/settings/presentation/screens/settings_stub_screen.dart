import 'package:flutter/material.dart';

import '../../../../shared/widgets/feature_stub_screen.dart';

class SettingsStubScreen extends StatelessWidget {
  const SettingsStubScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const FeatureStubScreen(
      featureTitle: 'Configurações',
      areaLabel: 'Área: Configurações — em construção',
    );
  }
}
