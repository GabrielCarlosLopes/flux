import 'package:flutter/material.dart';

import '../../../../shared/widgets/feature_stub_screen.dart';

class RoutinesStubScreen extends StatelessWidget {
  const RoutinesStubScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const FeatureStubScreen(
      featureTitle: 'Rotinas',
      areaLabel: 'Área: Rotinas — em construção',
    );
  }
}
