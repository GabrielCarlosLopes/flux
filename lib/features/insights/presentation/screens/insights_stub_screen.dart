import 'package:flutter/material.dart';

import '../../../../shared/widgets/feature_stub_screen.dart';

class InsightsStubScreen extends StatelessWidget {
  const InsightsStubScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const FeatureStubScreen(
      featureTitle: 'Insights',
      areaLabel: 'Área: Insights — em construção',
    );
  }
}
